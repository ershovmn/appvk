<%
  if params[:action] == 'index' || params[:action] == 'create'
    path = restaraunt_menu_categories_path(params[:restaraunt_id])
  else
    path = restaraunt_menu_category_path(params[:restaraunt_id], params[:id])
  end
%>

<%= form_with(model: menu_category, local: true, url: path) do |form| %>
  <% if menu_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(menu_category.errors.count, "error") %> prohibited this menu_category from being saved:</h2>

      <ul>
        <% menu_category.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
